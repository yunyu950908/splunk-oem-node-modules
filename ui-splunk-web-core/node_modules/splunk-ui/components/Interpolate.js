module.exports =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ({

/***/ 0:
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.default = Interpolate;
	
	var _react = __webpack_require__(1);
	
	var _react2 = _interopRequireDefault(_react);
	
	var _errorHandling = __webpack_require__(11);
	
	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
	
	function _objectWithoutProperties(obj, keys) { var target = {}; for (var i in obj) { if (keys.indexOf(i) >= 0) continue; if (!Object.prototype.hasOwnProperty.call(obj, i)) continue; target[i] = obj[i]; } return target; }
	
	var splitRegEx = /(\$+?)(\w+?)(\$)/;
	
	var propTypes = {
	    /**
	     * The target string for values to be interplated into. Use the syntax `$key$` to denote tokens
	     * to be replaced by a value from the values prop. Use a second '$' to prevent a value escape.
	     * Keys may contain letters, numbers, or underscore.
	     */
	    children: _react.PropTypes.string.isRequired,
	    /**
	     * A map of keys to renderable values to be interpolated into the passed string.
	     */
	    values: _react.PropTypes.objectOf(_react.PropTypes.node).isRequired,
	    /**
	     * A component or tag to be used for the container.
	     */
	    Tag: _react.PropTypes.any
	};
	
	var defaultProps = {
	    Tag: 'span'
	};
	
	/**
	 * THIS IS A PRELIMINARY IMPLEMENTATION AND WILL CHANGE AS WE DECIDE UPON A SYNTAX
	 * Interpolate swaps tokens for values from the values prop. This is useful in conjunction with
	 * `gettext` (or `_`) from the `splunk-ui/util/i18n` module. Translate strings with gettext then use
	 * Interpolate to inject values after translation.
	 */
	function Interpolate(props) {
	    var children = props.children,
	        Tag = props.Tag,
	        values = props.values,
	        other = _objectWithoutProperties(props, ['children', 'Tag', 'values']);
	
	    var elements = [];
	    var tokens = children.split(splitRegEx);
	    var i = 0;
	    while (i < tokens.length) {
	        var string = tokens[i];
	        var prefix = tokens[i + 1];
	        var token = tokens[i + 2];
	        var suffix = tokens[i + 3];
	        if (string) {
	            elements.push(string);
	        }
	        if (token) {
	            if (prefix.length === 1) {
	                var val = values[token];
	                if ((process.env.NODE_ENV !== "production") && val === undefined) {
	                    (0, _errorHandling.invariant)('Invalid token passed to Interpolate: Expected ' + token + ' to be\n                        defined');
	                }
	                if (typeof val === 'string') {
	                    elements.push(val);
	                } else {
	                    elements.push((0, _react.cloneElement)(val, {
	                        key: val.key || i
	                    }));
	                }
	            } else {
	                // Token has been escaped, put it back together and add it as a string
	                var _val = prefix.slice(1) + token + suffix;
	                elements.push(_val);
	            }
	        } else {
	            break;
	        }
	        i += 4;
	    }
	
	    return _react2.default.createElement(
	        Tag,
	        other,
	        elements
	    );
	}
	
	Interpolate.propTypes = propTypes;
	Interpolate.defaultProps = defaultProps;

/***/ },

/***/ 1:
/***/ function(module, exports) {

	module.exports = require("react");

/***/ },

/***/ 11:
/***/ function(module, exports) {

	module.exports = require("splunk-ui/util/errorHandling");

/***/ }

/******/ });
//# sourceMappingURL=Interpolate.js.map
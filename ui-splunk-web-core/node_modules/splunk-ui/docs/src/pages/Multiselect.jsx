import React from 'react';
import DocExample from 'components/DocExample';
import DocPage from 'components/DocPage';
import Specs from 'components/Specs';
import docs from '!!docgen-loader!splunk-ui/components/Multiselect';
import docsOption from '!!docgen-loader!splunk-ui/components/Multiselect/Option';
import packageData from 'splunk-ui/components/Multiselect/package.json';

const examples = require.context('splunk-ui/components/Multiselect/examples/');
const examplesRaw = require.context('!!raw!splunk-ui/components/Multiselect/examples/');

function Multiselect() {
    return (
        <DocPage {...packageData} description={docs[0].description}>
            <DocExample
                title="Controlled"
                description="Basic example."
                code={examplesRaw('./Controlled')}
                example={examples('./Controlled').default}
            />
            <DocExample
                title="Uncontrolled"
                description="Basic example."
                code={examplesRaw('./Uncontrolled')}
                example={examples('./Uncontrolled').default}
            />
            <DocExample
                title="New Values"
                description="Optionally, users can be allowed to add arbitrary values."
                code={examplesRaw('./NewValues')}
                example={examples('./NewValues').default}
            />
            <DocExample
                title="Error"
                code={examplesRaw('./Error')}
                example={examples('./Error').default}
            />
            <DocExample
                title="Disabled"
                code={examplesRaw('./Disabled')}
                example={examples('./Disabled').default}
            />
            <DocExample
                title="Fetching"
                code={examplesRaw('./Fetching')}
                example={examples('./Fetching').default}
            >
                It's possible to populate the Options from a server. Here, that behavior is
                simulated.
            </DocExample>
            <Specs docs={docs[0]} title="Multiselect" />
            <Specs docs={docsOption[0]} title="Multiselect.Option" />
        </DocPage>
    );
}

export default Multiselect;

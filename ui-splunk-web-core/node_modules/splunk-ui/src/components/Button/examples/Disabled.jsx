import React from 'react';
import Button from 'splunk-ui/components/Button';

function Buttons() {
    return (
        <div>
            <Button disabled label="default" />
            <Button disabled appearance="primary" label="primary" />
            <Button disabled appearance="pill" label="pill" />
        </div>
    );
}

export default Buttons;

import React from 'react';
import Button from 'splunk-ui/components/Button';
import Print from 'splunk-ui/icons/Print';

function Buttons() {
    return (
        <div>
            <Button icon={<Print screenReaderText={null} />} label="Print" />
            <Button icon={<Print screenReaderText={null} />} appearance="primary" label="Print" />
            <Button icon={<Print screenReaderText={null} />} appearance="pill" label="Print" />
        </div>
    );
}

export default Buttons;

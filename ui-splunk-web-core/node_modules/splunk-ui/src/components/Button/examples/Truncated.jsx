import React from 'react';
import Button from 'splunk-ui/components/Button';
import Print from 'splunk-ui/icons/Print';

function Buttons() {
    return (
        <div>
            <div style={{ maxWidth: '300px', display: 'inline-block' }}>
                <Button
                    inline={false}
                    style={{ marginBottom: '10px' }}
                    label="One one one one one one one one one one one one one one one"
                />
                <Button
                    inline={false}
                    style={{ marginBottom: '10px' }}
                    appearance="primary"
                    label="Two two two two two two two two two two two two two"
                />
                <Button
                    inline={false}
                    appearance="pill"
                    label="Three three three three three three three three three three three"
                />
            </div>
            <div style={{ maxWidth: '300px', marginLeft: '10px', display: 'inline-block' }}>
                <Button
                    inline={false}
                    style={{ marginBottom: '10px' }}
                    icon={<Print />}
                    isMenu
                    label="One one one one one one one one one one one one one one one"
                />
                <Button
                    inline={false}
                    style={{ marginBottom: '10px' }}
                    icon={<Print />} appearance="primary"
                    isMenu
                    label="Two two two two two two two two two two two two two"
                />
                <Button
                    inline={false}
                    appearance="pill"
                    icon={<Print />}
                    label="Three three three three three three three three three three three"
                    isMenu
                />
            </div>
        </div>
    );
}

export default Buttons;

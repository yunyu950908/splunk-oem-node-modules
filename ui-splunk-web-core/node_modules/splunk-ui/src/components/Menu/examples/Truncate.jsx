import React from 'react';
import Menu from 'splunk-ui/components/Menu';

function Basic() {
    const longLabel = 'A very long label truncation truncation truncation truncation truncation';

    return (
        <Menu style={{ maxWidth: '200px' }} retainFocus={false}>
            <Menu.Item label={longLabel} truncate />
            <Menu.Item label={longLabel} truncate />
            <Menu.Item label={longLabel} />
            <Menu.Item label={longLabel} />
            <Menu.Item label="aVeryLongLabelWordWordWordWordWordWordWordWordWordWordWordWordWord" />
        </Menu>
    );
}

export default Basic;

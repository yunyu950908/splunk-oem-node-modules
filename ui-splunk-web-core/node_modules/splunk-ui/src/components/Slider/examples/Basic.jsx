import React from 'react';
import Slider from 'splunk-ui/components/Slider';

function Basic() {
    return (
        <Slider min={100} max={500} step={25} defaultValue={300} />
    );
}

export default Basic;

if (!window.$C) {
    window.$C = {};
}
export const buildNumber = window.$C.BUILD_NUMBER;
export const buildPushNumber = window.$C.BUILD_PUSH_NUMBER;
export const config = window.$C;
export const locale = window.$C.LOCALE;
export const splunkdPath = window.$C.SPLUNKD_PATH;
export const username = window.$C.USERNAME;
